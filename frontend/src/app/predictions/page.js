"use client";

import { useState, useEffect, useCallback } from "react";
import {
  getPredictions,
  getPredictionStats,
} from "@/lib/api";
import {
  Search,
  Filter,
  CheckCircle,
  AlertTriangle,
  Clock,
  ArrowUpDown,
  ChevronLeft,
  ChevronRight,
  TrendingUp,
} from "lucide-react";

export default function PredictionsPage() {
  const [predictions, setPredictions] = useState([]);
  const [stats, setStats] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  // Filters & Pagination
  const [page, setPage] = useState(1);
  const [pageSize, setPageSize] = useState(25);
  const [delayStatus, setDelayStatus] = useState("");
  const [search, setSearch] = useState("");
  const [sortBy, setSortBy] = useState("id");
  const [sortOrder, setSortOrder] = useState("desc");
  const [totalPages, setTotalPages] = useState(1);
  const [totalCount, setTotalCount] = useState(0);

  useEffect(() => {
    let isMounted = true;
    async function loadData() {
      setLoading(true);
      setError(null);
      try {
        const [predData, statsData] = await Promise.all([
          getPredictions({
            page,
            pageSize,
            delayStatus,
            search,
            sortBy,
            sortOrder,
          }),
          getPredictionStats(),
        ]);

        if (isMounted) {
          setPredictions(predData.items || []);
          setTotalPages(predData.total_pages || 1);
          setTotalCount(predData.total || 0);
          setStats(statsData);
        }
      } catch (err) {
        if (isMounted) setError(err.message || "Failed to load predictions");
      } finally {
        if (isMounted) setLoading(false);
      }
    }

    loadData();
    return () => {
      isMounted = false;
    };
  }, [page, pageSize, delayStatus, search, sortBy, sortOrder]);

  const toggleSort = (field) => {
    if (sortBy === field) {
      setSortOrder(sortOrder === "asc" ? "desc" : "asc");
    } else {
      setSortBy(field);
      setSortOrder("desc");
    }
  };

  return (
    <div className="space-y-6">
      {/* Header */}
      <div>
        <h1 className="text-2xl font-bold text-white tracking-tight">Ride Order Predictions</h1>
        <p className="text-sm text-slate-400 mt-1">
          Historical ETA predictions & delay classifications generated by PyTorch engine
        </p>
      </div>

      {/* Stats Overview */}
      {stats && (
        <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
          <div className="glass-card p-5">
            <div className="flex items-center justify-between text-slate-400">
              <span className="text-xs font-semibold uppercase tracking-wider">Total Predictions</span>
              <Clock className="w-4 h-4 text-cyan-400" />
            </div>
            <p className="text-2xl font-bold text-white mt-2">{stats.total_predictions?.toLocaleString()}</p>
            <p className="text-xs text-slate-500 mt-1">Evaluated on test dataset</p>
          </div>

          <div className="glass-card p-5">
            <div className="flex items-center justify-between text-slate-400">
              <span className="text-xs font-semibold uppercase tracking-wider">Avg ETA Error</span>
              <TrendingUp className="w-4 h-4 text-purple-400" />
            </div>
            <p className="text-2xl font-bold text-white mt-2">
              {stats.avg_eta_error !== null ? `${stats.avg_eta_error} min` : "N/A"}
            </p>
            <p className="text-xs text-slate-500 mt-1">Mean absolute error (MAE)</p>
          </div>

          <div className="glass-card p-5">
            <div className="flex items-center justify-between text-slate-400">
              <span className="text-xs font-semibold uppercase tracking-wider">Predicted Delayed</span>
              <AlertTriangle className="w-4 h-4 text-rose-400" />
            </div>
            <p className="text-2xl font-bold text-rose-400 mt-2">{stats.total_delayed?.toLocaleString()}</p>
            <p className="text-xs text-slate-500 mt-1">
              {stats.total_predictions
                ? `${((stats.total_delayed / stats.total_predictions) * 100).toFixed(1)}% of total`
                : "0%"}
            </p>
          </div>

          <div className="glass-card p-5">
            <div className="flex items-center justify-between text-slate-400">
              <span className="text-xs font-semibold uppercase tracking-wider">Predicted On-Time</span>
              <CheckCircle className="w-4 h-4 text-emerald-400" />
            </div>
            <p className="text-2xl font-bold text-emerald-400 mt-2">{stats.total_on_time?.toLocaleString()}</p>
            <p className="text-xs text-slate-500 mt-1">
              {stats.total_predictions
                ? `${((stats.total_on_time / stats.total_predictions) * 100).toFixed(1)}% of total`
                : "0%"}
            </p>
          </div>
        </div>
      )}

      {/* Filter Controls */}
      <div className="glass-card p-4 flex flex-col md:flex-row gap-4 items-center justify-between">
        {/* Search */}
        <div className="relative w-full md:w-80">
          <Search className="absolute left-3 top-2.5 w-4 h-4 text-slate-400" />
          <input
            type="text"
            placeholder="Search Order ID..."
            value={search}
            onChange={(e) => {
              setSearch(e.target.value);
              setPage(1);
            }}
            className="w-full pl-9 pr-4 py-2 bg-slate-900/80 border border-slate-700/60 rounded-xl text-sm text-slate-200 placeholder-slate-500 focus:outline-none focus:border-cyan-500"
          />
        </div>

        <div className="flex flex-wrap items-center gap-3 w-full md:w-auto">
          {/* Status Filter */}
          <div className="flex items-center space-x-2 bg-slate-900/80 border border-slate-700/60 rounded-xl p-1">
            <button
              onClick={() => {
                setDelayStatus("");
                setPage(1);
              }}
              className={`px-3 py-1.5 text-xs font-medium rounded-lg transition-colors ${
                delayStatus === ""
                  ? "bg-cyan-500/20 text-cyan-300 border border-cyan-500/40"
                  : "text-slate-400 hover:text-slate-200"
              }`}
            >
              All
            </button>
            <button
              onClick={() => {
                setDelayStatus("delayed");
                setPage(1);
              }}
              className={`px-3 py-1.5 text-xs font-medium rounded-lg transition-colors ${
                delayStatus === "delayed"
                  ? "bg-rose-500/20 text-rose-300 border border-rose-500/40"
                  : "text-slate-400 hover:text-slate-200"
              }`}
            >
              Delayed
            </button>
            <button
              onClick={() => {
                setDelayStatus("on_time");
                setPage(1);
              }}
              className={`px-3 py-1.5 text-xs font-medium rounded-lg transition-colors ${
                delayStatus === "on_time"
                  ? "bg-emerald-500/20 text-emerald-300 border border-emerald-500/40"
                  : "text-slate-400 hover:text-slate-200"
              }`}
            >
              On-Time
            </button>
          </div>

          {/* Page Size Select */}
          <select
            value={pageSize}
            onChange={(e) => {
              setPageSize(Number(e.target.value));
              setPage(1);
            }}
            className="bg-slate-900/80 border border-slate-700/60 rounded-xl px-3 py-2 text-xs text-slate-300 focus:outline-none focus:border-cyan-500"
          >
            <option value={15}>15 per page</option>
            <option value={25}>25 per page</option>
            <option value={50}>50 per page</option>
            <option value={100}>100 per page</option>
          </select>
        </div>
      </div>

      {/* Error Message */}
      {error && (
        <div className="p-4 bg-rose-500/10 border border-rose-500/30 rounded-xl text-rose-300 text-sm">
          {error}
        </div>
      )}

      {/* Predictions Table */}
      <div className="glass-card overflow-hidden">
        <div className="overflow-x-auto">
          <table className="w-full text-left text-sm text-slate-300">
            <thead className="bg-slate-900/90 text-xs uppercase text-slate-400 font-semibold border-b border-slate-800">
              <tr>
                <th
                  onClick={() => toggleSort("order_id")}
                  className="px-4 py-3.5 cursor-pointer hover:text-slate-200 transition-colors"
                >
                  <div className="flex items-center space-x-1">
                    <span>Order ID</span>
                    <ArrowUpDown className="w-3 h-3" />
                  </div>
                </th>
                <th className="px-4 py-3.5">Booking Timestamp</th>
                <th
                  onClick={() => toggleSort("actual_total_time_minutes")}
                  className="px-4 py-3.5 cursor-pointer hover:text-slate-200"
                >
                  Actual ETA
                </th>
                <th className="px-4 py-3.5">Google ETA</th>
                <th
                  onClick={() => toggleSort("predicted_eta_minutes")}
                  className="px-4 py-3.5 cursor-pointer hover:text-slate-200 text-cyan-400"
                >
                  Model Predicted ETA
                </th>
                <th className="px-4 py-3.5">ETA Error</th>
                <th
                  onClick={() => toggleSort("predicted_delay_probability")}
                  className="px-4 py-3.5 cursor-pointer hover:text-slate-200"
                >
                  Delay Probability
                </th>
                <th className="px-4 py-3.5 text-center">Prediction</th>
                <th className="px-4 py-3.5 text-center">Ground Truth</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-slate-800/60">
              {loading ? (
                Array.from({ length: 8 }).map((_, idx) => (
                  <tr key={idx} className="animate-pulse">
                    <td className="px-4 py-4"><div className="h-4 bg-slate-800 rounded w-24"></div></td>
                    <td className="px-4 py-4"><div className="h-4 bg-slate-800 rounded w-32"></div></td>
                    <td className="px-4 py-4"><div className="h-4 bg-slate-800 rounded w-16"></div></td>
                    <td className="px-4 py-4"><div className="h-4 bg-slate-800 rounded w-16"></div></td>
                    <td className="px-4 py-4"><div className="h-4 bg-slate-800 rounded w-16"></div></td>
                    <td className="px-4 py-4"><div className="h-4 bg-slate-800 rounded w-16"></div></td>
                    <td className="px-4 py-4"><div className="h-4 bg-slate-800 rounded w-20"></div></td>
                    <td className="px-4 py-4 text-center"><div className="h-6 bg-slate-800 rounded-full w-16 mx-auto"></div></td>
                    <td className="px-4 py-4 text-center"><div className="h-6 bg-slate-800 rounded-full w-16 mx-auto"></div></td>
                  </tr>
                ))
              ) : predictions.length === 0 ? (
                <tr>
                  <td colSpan={9} className="text-center py-12 text-slate-500">
                    No predictions found. Run predictions or upload test data.
                  </td>
                </tr>
              ) : (
                predictions.map((row) => {
                  const errorMin = row.predicted_eta_minutes && row.actual_total_time_minutes
                    ? Math.abs(row.predicted_eta_minutes - row.actual_total_time_minutes).toFixed(2)
                    : "N/A";
                  const probPercent = row.predicted_delay_probability !== null
                    ? (row.predicted_delay_probability * 100).toFixed(1)
                    : "0.0";

                  const isMatch = row.predicted_delay === row.order_delayed;

                  return (
                    <tr
                      key={row.id}
                      className="hover:bg-slate-800/40 transition-colors font-mono text-xs"
                    >
                      <td className="px-4 py-3.5 font-semibold text-slate-200">{row.order_id}</td>
                      <td className="px-4 py-3.5 text-slate-400 font-sans text-xs">{row.booking_timestamp || "-"}</td>
                      <td className="px-4 py-3.5 font-bold text-slate-200">
                        {row.actual_total_time_minutes?.toFixed(1)} min
                      </td>
                      <td className="px-4 py-3.5 text-slate-400">
                        {row.google_total_eta_minutes?.toFixed(1)} min
                      </td>
                      <td className="px-4 py-3.5 font-bold text-cyan-400">
                        {row.predicted_eta_minutes?.toFixed(1)} min
                      </td>
                      <td className="px-4 py-3.5">
                        <span className={`px-2 py-0.5 rounded ${
                          Number(errorMin) <= 3.0
                            ? "bg-emerald-500/10 text-emerald-400"
                            : Number(errorMin) <= 7.0
                            ? "bg-amber-500/10 text-amber-400"
                            : "bg-rose-500/10 text-rose-400"
                        }`}>
                          ±{errorMin}m
                        </span>
                      </td>
                      <td className="px-4 py-3.5">
                        <div className="flex items-center space-x-2">
                          <div className="w-16 bg-slate-800 rounded-full h-1.5 overflow-hidden">
                            <div
                              className={`h-full ${
                                row.predicted_delay_probability >= 0.5
                                  ? "bg-rose-500"
                                  : row.predicted_delay_probability >= 0.3
                                  ? "bg-amber-500"
                                  : "bg-emerald-500"
                              }`}
                              style={{ width: `${Math.min(100, probPercent)}%` }}
                            ></div>
                          </div>
                          <span>{probPercent}%</span>
                        </div>
                      </td>
                      <td className="px-4 py-3.5 text-center">
                        <span
                          className={`inline-flex items-center px-2.5 py-1 rounded-full text-[11px] font-semibold font-sans ${
                            row.predicted_delay === 1
                              ? "bg-rose-500/15 text-rose-400 border border-rose-500/30"
                              : "bg-emerald-500/15 text-emerald-400 border border-emerald-500/30"
                          }`}
                        >
                          {row.predicted_delay === 1 ? "Delayed" : "On Time"}
                        </span>
                      </td>
                      <td className="px-4 py-3.5 text-center">
                        <span
                          className={`inline-flex items-center px-2 py-0.5 rounded text-[11px] font-sans ${
                            isMatch
                              ? "text-slate-400"
                              : "text-amber-400 bg-amber-500/10 border border-amber-500/20"
                          }`}
                        >
                          {row.order_delayed === 1 ? "Delayed" : "On Time"}
                          {!isMatch && <span className="ml-1 text-[10px]">⚠️</span>}
                        </span>
                      </td>
                    </tr>
                  );
                })
              )}
            </tbody>
          </table>
        </div>

        {/* Pagination Footer */}
        <div className="p-4 bg-slate-900/60 border-t border-slate-800/80 flex flex-col sm:flex-row items-center justify-between gap-4 text-xs text-slate-400">
          <div>
            Showing <span className="font-semibold text-slate-200">{predictions.length}</span> of{" "}
            <span className="font-semibold text-slate-200">{totalCount.toLocaleString()}</span> predictions
          </div>
          <div className="flex items-center space-x-2">
            <button
              onClick={() => setPage((p) => Math.max(1, p - 1))}
              disabled={page === 1 || loading}
              className="p-2 bg-slate-800 hover:bg-slate-700 disabled:opacity-40 disabled:cursor-not-allowed rounded-lg transition-colors"
            >
              <ChevronLeft className="w-4 h-4" />
            </button>
            <span className="px-3 py-1 bg-slate-800/60 border border-slate-700/60 rounded-lg text-slate-200">
              Page {page} of {totalPages}
            </span>
            <button
              onClick={() => setPage((p) => Math.min(totalPages, p + 1))}
              disabled={page >= totalPages || loading}
              className="p-2 bg-slate-800 hover:bg-slate-700 disabled:opacity-40 disabled:cursor-not-allowed rounded-lg transition-colors"
            >
              <ChevronRight className="w-4 h-4" />
            </button>
          </div>
        </div>
      </div>
    </div>
  );
}
